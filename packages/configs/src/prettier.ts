import type { Config } from 'prettier';

const config: Config = {
    semi: true,
    singleQuote: true,
    printWidth: 100,
    tabWidth: 4,
    trailingComma: 'es5',
    bracketSpacing: true,
};

export default config;
